import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Nunito } from "next/font/google";



export const metadata: Metadata = {
  title: 'USA Super Stores | Discount Grocery in Upper East Side NYC',
  description: 'Find affordable everyday essentials at USA Super Stores in the Upper East Side. We offer a wide variety of groceries, home goods, and snacks at the best prices in NYC.',
  openGraph: {
    "title": "USA Super Stores | Affordable Groceries NYC",
    "description": "Wide variety of products, unbeatable prices, and friendly service in the Upper East Side.",
    "siteName": "USA Super Stores",
    "type": "website"
  },
};

const nunito = Nunito({
  variable: "--font-nunito",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${nunito.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
